apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: cloud-native-platform
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: order-service
