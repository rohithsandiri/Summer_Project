apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: cloud-native-platform
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: payment-service
