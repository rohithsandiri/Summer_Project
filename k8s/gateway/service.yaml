apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: cloud-native-platform
  labels:
    app: api-gateway
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: api-gateway
